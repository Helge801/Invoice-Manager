<UserControl
    x:Class="Custom_Air_Files.UserControls.UnitListUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Custom_Air_Files.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Style="{StaticResource GeneralInformationTextBlockStyle}"
                       FontWeight="Bold"
                       Text="{x:Bind Unit.Name}"/>
            <TextBlock Style="{StaticResource GeneralInformationTextBlockStyle}"
                       Text="{x:Bind Unit.Type}"/>
        </StackPanel>
    </Grid>
</UserControl>
